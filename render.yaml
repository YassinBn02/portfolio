services:
  - type: web
    name: portfolio
    env: node
    buildCommand: |
      npm config set package-lock false
      rm -f pnpm-lock.yaml
      npm install --force
      npm run build
    staticPublishPath: ./out
    node:
      version: 18.x
    build:
      useYarn: false
      usePnpm: false