services:
  - type: web
    name: portfolio
    env: node
    buildCommand: npm install --force && npm run build
    staticPublishPath: ./out
    node:
      version: 18.x
    build:
      useYarn: false
      usePnpm: false